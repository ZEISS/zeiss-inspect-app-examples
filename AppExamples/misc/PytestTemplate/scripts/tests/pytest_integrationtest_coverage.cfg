# pytest configuration
# see https://docs.pytest.org/en/stable/reference/reference.html#ini-options-ref
[tool:pytest]

# Default: <AppDir>/scripts/tests/.pytest_cache
# Change if App is finalized (read-only)
#cache_dir = /path/to/.pytest_cache

addopts = --cov=uut_project_keywords --cov-report html:integrationtest-coverage --cov-report xml:integrationtest-coverage.xml

# Generate coverage report for multiple independent Python modules, e.g. "script1.py" and "script2.py":
#addopts = --cov=script1 --cov=script2 --cov-report html:integrationtest-coverage --cov-report xml:integrationtest-coverage.xml
